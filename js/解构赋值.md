### 解构赋值

```javascript
let data = {
    username : 'xiaoming',
    age:18
}
//解构赋值
let {username,age} = data;

//赋值 
let username = data.username;
let age = data.age; 

//解构赋值 和 赋值 的效果是相同的，但是解构赋值， 比较优雅；
```

