#### day2

> 大纲：
>
> 1. 上传相关知识点
> 2. 下载
> 3. excel

### 知识点总汇

1.  js 基础面试题
2. html基础面试题
3. css基础面试题
4. vue基础
5. ajax 
6. es6相关知识点
7. web开发的兼容性
8. 技术栈有哪些  对应的每个技术栈都有什么内容
9. 跨域相关
10. http
11. 异步回调地狱的解决方案等....
12. 表达自己的项目所做的模块的完整性
13. jquery相关的必备语法
14. webpack配置相关的一些知识点；



###  导航守卫的应用场景

1.  导航守卫是用来在跳转路由之前进行判断，一般是用来对角色管理进行权限判断
2. 例如： 如果是客户端，直接拿到某供应商的店铺管理的url地址，如果他直接去访问，则会跳转到该页面去，然后我们通过axios拦截器，去调用服务端的获取店铺接口，该接口最后返回没有权限访问， 紧接着在跳出当前页面；
3. 如果你看懂上面的流程后，你会发现一个非常不好的用户体验，是什么呢？
4. 客户本身没有办法去访问商家的店铺管理，但是我们让客户跳转到了商品的店铺管理页面，这本身就不合理；
5. 此时我们就需要在用户进入店铺之前，利用导航守卫 直接判断该用户的权限 ，如果没有，直接页面不都不让他跳转
6. 这就是他的应用场景之一
7. 应用场景之二是： 为了极致的用户体验，有的人喜欢把 created()或者mounted()之前的所有逻辑，写到跳转路由之前去加载；
8. 这样用有一个问题，代码可读性变差，
9. 其实在vue3中自带解决此方案的问题，
10. 所以如果你遇到这种写法，他只适用于vue2.x
11. 总结 ： 1. 用来判断角色权限，根据不同的角色，进行不同的拦截 2：可以把created,mounted()钩子函数的逻辑，放在组件导航守卫中，以提高页面的性能；

#### 能说一下你对this指向的理解吗？

答：在js中，谁调用，this就是谁；

####  你知道call,apply，bind的作用及应用场景吗？

答： 

#### 如何改变this指向？

答： call ,apply,bind

#### 你知道call,apply,bind的区别吗？

答： 

####  在你的项目中，如何做上传的？

答： 上传文件时，我们是用流的方式来上传；

上传时，前端利用 new FormData()对象，把文件内容放在此对象中，发给服务端；

上传时，用的格式，不是application/json 而是 multpart/form-data

```javascript
var vm = new Vue({
    data(){
        
    },
    methods:{
        upload(){
            let formData = new FormData();
            //this.file，就是我们上传过后的那个文件
            formData.append('file',this.file); 
            let res = await uploadAvatar(formData)
            if(res.status == 1){
                alert('上传成功')
            }
        }
    }
})
```

#### 下载

答： 下载时，是请求服务端的接口，该接口是get请求，

```html
<a href="www.lol.com/download">下载</a>
```

#### 导出

1. echarts导出，他是echarts自带的导出图的方法；直接调用echarts API就可以实现
2. 还有一种通常在后台管理中，导出的是excel文件；
3. 导出如何做？ 通常情况下，我们在vue项目中，用到了一个excel组件，该组件（xlsx）就可以自定义导出的内容；
4. 如何自定义导出的表头？
5. 方法一：页面中展示一个与产品需求对应的table，自己在创建一个页面中看不见的和导出时长的一模一样的table，只是这个看不见，被隐藏掉了，用户导出的时候，导出的其实是看不见的这个表格；
6. 方法二：利用原生js，克隆表格的dom元素，用原生js，删除或者修改被克隆的表格，进行导出

#### 项目相关的疑难杂症

1. 你们C端项目上线了吗？

2. 上线了多长时间，

3. 我可以访问你的线上地址吗？

4. 你做这个项目多长时间了？

   1. 一般情况下，一个项目在6-15 - 24个月之间都ok；
   2. 比如你做的主要项目，可以说在15-20个月左右，你做的项目，最少要说4-6-8个模块 
   3. 但是你刚介入一个项目，通常情况下在2-3-4-5-6个月；

5. 你们做两个模块需要维护这么长时间吗？

   ​	如果一个面试官问这道题的时候，你就很危险了；

   ​	如果问你这些题，就是觉得你说的有问题了；

#### 项目模块的开发时长相关？

正常情况下，你去维护一个模块，可以维护好长时间，因为一个模块本质上可以 说成是一个单独的项目， 只是使用人群的类型不一样，就造成的 工作量也不一样； 用户只要有需求，开发就可以在上面加需求； 需求是无止境的；

通常情况下，我们做项目时，可以说4-6个模块；

开发时间一般控制在6-15-20个月之间；

####  3年内你一共换过几家公司？

一般情况下是1-2家，最好是1家，因为你换的公司越少， 公司觉得你的稳定性越高

如果你面试京东，阿里，百度，相关的大公司， 他会要求5年内最多从适2家公司，如果在多，他就不会要你；

所以，你要你做的项目中，一定要够维持3年；

一般情况下可以多说几个项目， 一般情况下， 3年可以做到4-6个项目，项目的类型几乎都差不多，

一般情况下，都是B端 和C端； 

一般情况下，项目并不是 维护完一个在维护另外 一个，几乎可以是同时开发；

#### 项目迭代周期

平均是一个月一次大版本更新，中间有可能参杂一次小版本更新；

#### 需求评审的时长

一般情况下，后台管理项目，一个table 表格  + 分页是 0.5人天  加上搜索校验相关的 0.5-1人天不等；通常情况下还会有0.5人天和服务端连调的时间； 所以你看见的一个正常的不太复杂的列表渲染，通常情况下是1.5人天 

如果遇到相对来说比较复杂的增加相关的东西，其中业务逻辑相对复杂 ，不止是渲染一个列表这么简单，通常情况下是2-4人天，具体时长根据不同的项目和业务需求来定；

#### 目前xx模块相对较火，为什么没有做呢？

答： 我已经听说产品同学对此已经在做产品调研，相信不久就会做这些功能

注： 产品会把当前最火的一些项目要求都会列入年计划表中，他有可能不会和你；

#### 如何描述自己的项目相关模块？

表达技巧： 表达模块时，先表达这个模块的作用，然后在表达这个模块中的功能*

比如订单模块，你可以说这个模块是让供应商来查看自己的订单销量情况；

可以根据订单列表查看订单相关信息；

在这个模块中，有订单列表，分页，按条件查询等相关功能；

 该页面支持按 订单号查询  配置快递公司查询 等。

#### 如何回答类似这种问题：请你说一下对xx的了解？

答： 先回答xx的概念，然后在说场景方面的应用；

比如问请你说一下对cookie的理解 ？

就可以回答说： cookie和sessionStorage，localStorage都是一种存储，

cookie通常都可以用来做登录相关的功能，

但是cookie相对于jwt来说，不可以跨域

注： 在以上回答中，你回答了cookie是一种存储，提到一seesionStorage,也提到了localStorage，

也说明了cookie的使用场景，就是用来做登录的，如果你的答案中有 概念，也有使用场景，那么这道题就已经很ok了； 

#### 请说一下对axios拦截器的作用？

axios拦截器分为两种，请求拦截器和响应拦截器，请求拦截器是用来在前端请求服务端接口时做的一个拦截，响应拦截器是在服务端返回数据时进行拦截；

通常情况下，请求拦截器用来对请求头进行公共的设置；

通常情况下，响应拦截器用来做一些登录失效相关的公共业务逻辑；

注： 此题的规则 就是遵循着上面的  你对xx的理解规范来答的，有概念，有使用场景，所以就ok，

在答任何一道 题时，都应该有  概念 + 使用场景；

#### 你之前做过jquery的项目吗？

答： 之前做过一些，但是时间比较长，之前做过一个xx官网，（此句可以省略：但是相对来说比较简单）

注： 因为我说过，jquery适合做一些官网项目，并且官网很简单，

在jquery中，常用的语法就那么几个;

#### Vue全家桶是什么？

答： vue-router  vuex  less

####  你的这项目是一个很大的电商项目吗？

答： 

#### 能访问一下你C端电商的项目地址吗？

答：  不能，因为我们的移动端项目是嵌套到第三方的app里面的，不可以单独运行；

#### jquery如何做后台管理项目？

利用iframe搭建 (不是用tab切换);

####  jquery后台管理技术栈选择

jquery  或者   jquery  + webpack

#### 你简单说一下跨域登录

答： 跨域登录是在前后端分离的项目中，前端做登录时的一种应用场景

#### 在你的工作中有没有遇到一些数据处理相关的问题？

答： 几年前会有，最近没有发现；

#### 你最近有没有遇到过一些疑难问题？

答：  如果你不知道说什么疑难问题，则可以说： 我不知道您说的哪一方面，你可以说的在具体一点；

或者直接说  最近遇到了 一些xxx样式兼容性问题， xxx js兼容问题   xxx elementui兼容性问题或者某技术栈的一些坑

#### 在你的项目中有用到过Promise吗？

答：  如果你说之前做过jquery项目的话，你可以说利用promise,和ajax在项目有过对服务端api进行封装过； 

如果你做的vue的项目，可以说，因为axios已经基于promise，所以没有二次使用promise;

#### 详细说onclick如何用到了闭包？

答： onclick事件的event入参就是通过js底层的闭包函数传过来的；

####  您项目中遇到过哪些相对复杂的功能？

答：  一般要从 项目的业务复杂度上说，说出来尽可能的详细，尽可能的复杂，千万不要只说两句话，这样别人认为不复杂；

#### vue组件相关知识

组件是vue自带的一个组件；vue组件底层的实现是基于原生的web Component;

但是在封装组件时，我们用的组件是Vue本身自带的，而不是原生js的组件；

#### 说一下对Promise的理解 

答：promise是解决地狱回调的一种方案，他接收一个回调函数，该函数有两个值 ，一个是resolve,

一个是reject， 当异步接口调用成功时，走resolve,失败的话，走reject;

#####  Promise的在执行过程中有几种状态？分别是什么？

答：  有三种  ；   pending (等待中)  fulfilled（成功）、rejected(拒绝，失败)

#### 你的项目中遇到过哪些疑难问题？  或者问你  你有没有遇到一些兼容性的问题？

答： https://www.cnblogs.com/phoebeyue/p/9226537.html



#### 炫萌优品（小帆帆）

答： 炫萌优品这个模块，是店家中的一个优品模块，他属于当前店家中的一个推荐分类，

在这个模块中，可以编辑商品到当前的炫萌优品分类中，可以进行对应的维护，也可以进行一些增删改查相关的业务流程； 一般可以编辑当前商品的主图或者banner图等；还可以对其进行设置打折和促销等活动，在此模块中，根据不同商家的要求，该模块还支持 满减送等活动；

> 注： 表达模块的时候，要表达清楚为什么要做这个模块，这个模块给谁来用，这个模块中有哪些知识点？      模块就属于商品推荐的一种模块，   配置这个模块可以让c端的用户来看对到应的商品；该商品的图肯定和其他店铺的商品图不一样；    增删改查相关的业务流程

#### 你的项目中有用到过无限级联动吗？

答： 在我们的项目中，有一个商品管理模块，在创建商品模块时，可以针对不同的商品选择不同的分类，我们的分类维度有可能是多级，在这个功能中，用到了无限级联动

> 注： 无限级联动 通常是 每选一个下拉框，就会调用一下服务端的接口，把当前分类的级id传给服务端，服务端如果返回了对应的数据列表，就说明有子级，如果返回的data.length为0则说明已经没有子级联动了；

#### 如果面试官问了一些 比如  你xx模块中用到了哪些技术时，该如何表达？

答： 表达的时候，尽量把技术栈说的自然，或者云淡风轻一点，不要说的太重视；

比如说： 你之前做文章模块时，用到了哪些技术点呢？

你就可以说： 文章模块和其它项目的文章模块一样，都是用到了那些个点，无非就是markdown解析和富文本解析方法两种；我们这个项目因为要考虑到商户并不熟悉markdown语法，所以选择了富文本；

>  注： 以上这种答法，你表明了，在你做项目时，有考虑到用户的感受，而特意做了一些技术选型；
>
> 但是如果你只说 你在这个模块中用到了富文本，就会显得比较单调；

#### 后台管理中角色的分类？

答： 供应商管理，采购商管理，渠道商管理，只要管理的是商户，那么该管理就应该是平台

比如动态路由的跳转，的权限管理，也应该是平台来做；

其它的大部分模块都是让 2b的商户来使用的；

#### 如何表达之前写过的jquery项目？

答： jquery是很早之前做的，做过一个后台管理,或者说是官网相关的；

####  你能说一下jquery如何做分页吗？

答： 之前有一个项目列表，列表中有分页的功能，分页是服务端来做的，当时用了一个分页插件，是  pagination.js,使用方法：

```javascript
var page = new Pagination('#pagination')  // 这样就可以在jquery中，实现类似elementui中的分页效果了；
```

